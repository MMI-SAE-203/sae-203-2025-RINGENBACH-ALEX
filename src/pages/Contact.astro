---
import Layout from "../layouts/Layout.astro";
---

<Layout>
    <section class="max-w-3xl mx-auto py-12 px-6 text-green-900">
        <h1 class="text-4xl font-bold text-center mb-8">Contact</h1>
        <p class="text-center text-lg mb-12">
            Une question sur le festival <strong>Ciné'Nature</strong> ? Remplissez
            le formulaire ci-dessous ou contactez-nous directement !
        </p>

        <!-- Formulaire de contact -->
        <form class="bg-white shadow-md rounded-lg p-6 space-y-6">
            <h2 class="text-2xl font-semibold">Envoyez-nous un message</h2>

            <div class="space-y-4">
                <input
                    type="text"
                    placeholder="Nom et prénom"
                    class="w-full px-4 py-3 border rounded-lg"
                    disabled
                />
                <input
                    type="email"
                    placeholder="Adresse email"
                    class="w-full px-4 py-3 border rounded-lg"
                    disabled
                />
                <textarea
                    placeholder="Votre message"
                    rows="4"
                    class="w-full px-4 py-3 border rounded-lg"
                    disabled></textarea>
            </div>

            <!-- Bouton envoi désactivé -->
            <button
                type="button"
                class="w-full py-3 text-lg font-semibold text-white bg-gray-400 rounded-lg cursor-not-allowed"
                disabled
            >
                Envoi indisponible
            </button>
            <p class="text-center text-gray-500 italic">
                Le formulaire n'est pas encore activé.
            </p>
        </form>

        <!-- Informations de contact -->
        <div class="mt-12 text-center">
            <h2 class="text-2xl font-semibold mb-4">Nos coordonnées</h2>
            <p>
                <strong> Email :</strong>
                <a
                    href="mailto:contact@cine-nature.com"
                    class="text-green-600 hover:underline"
                    >contact@cine-nature.com</a
                >
            </p>
            <p>
                <strong> Téléphone :</strong>
                <a
                    href="tel:+33123456789"
                    class="text-green-600 hover:underline">+33 3 84 90 90 10</a
                >
            </p>
            <p>
                <strong>Adresse :</strong> Lac du Malsaucy, 90000 Belfort, France
            </p>
        </div>
    </section>
</Layout>
